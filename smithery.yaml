startCommand:
  type: stdio
  configSchema:
    type: object
  commandFunction: |-
    config => ({
      command: 'python',
      args: ['-m','mcp-metal-price'],
      env: {
        GOLDAPI_API_KEY: process.env.GOLDAPI_API_KEY
      }
    })